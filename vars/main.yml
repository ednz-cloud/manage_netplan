---
# vars file for manage_netplan
manage_netplan_packages:
  - name: netplan.io
    version: latest
    state: present
manage_netplan_networkmanager_pkg:
  - name: network
    version: latest
    state: present
