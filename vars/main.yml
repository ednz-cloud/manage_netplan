---
# vars file for manage_netplan

# title: Netplan packages
# required: false
# description: List of packages necessary for Netplan functionality
manage_netplan_packages:
  - name: netplan.io
    version: latest
    state: present

# title: NetworkManager packages
# required: false
# description: List of packages required to enable NetworkManager functionality
manage_netplan_networkmanager_pkg:
  - name: network
    version: latest
    state: present
